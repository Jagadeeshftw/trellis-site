# Public URL of the deployed site, used for canonical links and Open Graph tags.
NEXT_PUBLIC_WEBSITE_URL=https://trellis.0xo.in

# Aceternity UI Pro registry key. Read by the shadcn CLI through components.json
# (npx shadcn add @aceternity/<component>). Never committed; keep it in .env.
ACETERNITY_UI_API_KEY=
